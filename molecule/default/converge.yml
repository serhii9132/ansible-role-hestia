---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: "Include ansible-role-hestia"
      ansible.builtin.include_role:
        name: "../../../ansible-role-hestia/" # noqa: role-name
